[agents]
max_threads = 4
max_depth = 1

# Project roles are discovered from .codex/agents/*.toml. Keep each role's name,
# description, and instructions in that one file instead of duplicating them as
# [agents.<role>] entries here.
